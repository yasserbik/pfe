server:
  port: 8086
  servlet:
    context-path: /pfe
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/pfe
    username: root
    password: root
  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: create

logging:
  level:
    ma.geo.local: INFO