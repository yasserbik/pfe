server:
  port: 8088
  servlet:
    context-path: /pfe
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3308/pfe
    username: root
    password: root
  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: create
logging:
  level:
    org.springframework: INFO
    ma.geo.local: ERROR
    org.hibernate: ERROR
    org.apache: ERROR
    com.zaxxer: ERROR